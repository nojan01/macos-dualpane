{
  "name": "dualbeam",
  "private": true,
  "version": "0.4.0",
  "license": "MIT",
  "author": "N.J.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "bash scripts/fetch-rclone.sh && tauri dev",
    "tauri:build": "bash scripts/fetch-rclone.sh && tauri build",
    "tauri:build:public": "bash scripts/fetch-rclone.sh && tauri build --no-default-features",
    "test": "vitest run",
    "rclone:fetch": "bash scripts/fetch-rclone.sh"
  },
  "dependencies": {
    "@crabnebula/tauri-plugin-drag": "^2.1.0",
    "@tauri-apps/api": "^2",
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "jsdom": "^29",
    "typescript": "~6.0.3",
    "vite": "^8.1.0",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.9"
  }
}
